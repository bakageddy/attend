{{page-title "Batch"}}

<main class="container batch__main">
  <div class="batch_wrapper">
    <Batch class="batch__crud">
      <Batch::Create/>
      <Batch::Delete/>
    </Batch>
    <Search @fields={{array "batch" "student"}}/>
  </div>
  <Result/>
  <Batch class="batch__data__crud">
    <Batch::Drop/>
    <Batch::DataCrud/>
  </Batch>
  {{yield}}
</main>